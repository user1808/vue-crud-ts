export default interface Person {
    id?: string,
    firstName: string;
    lastName: string;
    email: string;
    phone: string;
    birthDate: string;
    fullName?: string;
    initials?: string;
}
